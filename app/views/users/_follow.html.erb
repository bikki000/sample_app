<%= form_for(current_user.relationships.build(followed_id: @user.id)) do |form| %>
	<div>
		<%= form.hidden_field :followed_id %>
	</div>
	<%= form.submit "Follow", class: "btn btn-primary btn-large" %>
<% end %>